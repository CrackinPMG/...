run-js-on-prompt.js application/javascript
(() => {
    window.addEventListener('keydown', (e) => {
        if (e.ctrlKey && e.key === '`') {
            const jsCode = prompt('');
            if (jsCode) eval(jsCode);
        }
    });
})();
